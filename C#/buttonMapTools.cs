using System;

namespace ButtonArray
{
  class Program
  {
    static void Main(string[] args)
    {
        
      string[] mapToTools;
      summerStrut = new string[] {"Dig", "nmap", "dirb", "airmon", "nikto", "hydra", "tracert"};

      int[] callOrder = {1, 2, 3, 4, 5, 6, 7};
    }
    static bool CallTool(int, num => int == 1.. //it's getting late and I need to map the libraries with a parallel mapping to GPIO for tools.
  }
}
